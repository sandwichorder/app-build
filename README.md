# Sorion sandwich order web page

## Deploy

After completing a build in directory of app-src/ using Git Bash
```
cd www/
git add --all
git commit -m "update"
git push
```
This will update https://sandwichorder.github.io/ .
